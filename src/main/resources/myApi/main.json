{
	"version":"1"
	,"description":"Ver.1 coding sample - /myApi/main?seq=1"
	,"process":[
		{"var1":"SELECT * FROM board WHERE seq={:params.seq}"}
		,{"var2":"SELECT * FROM board WHERE seq={:var1[0].seq}"}
		]
	,"return":"SELECT * FROM board WHERE seq={:params.seq}"
}